## 1. 实施
- [x] 1.1 评估 Expo SDK 54 迁移影响与版本矩阵（Expo/React/React Native/库依赖）。
- [x] 1.2 更新 `package.json` 与 Expo 配置（`app.json`/`babel.config.js`）到 SDK 54 对应版本。
- [x] 1.3 处理 SDK 54 兼容性调整（运行时警告、API 变更、平台差异）。
- [x] 1.4 实现近一年拉取进度展示与失败保留缓存的 UI/状态处理。
- [x] 1.5 实现阈值输入的非法值提示与回退逻辑。
- [x] 1.6 更新或补充测试（如 `tests/attendance.test.ts`），覆盖新增行为。
- [ ] 1.7 验证：`npm test` 与 `expo start` 基础启动检查。
